<?xml version="1.0" encoding="UTF-8" ?>
<odoo>

    <!-- Action of Plan Users-->
    <record id="action_plan_users" model="ir.actions.act_window">
        <field name="name">Users</field>
        <field name="res_model">subscription.user</field>
        <field name="view_mode">tree,form</field>
    </record>

    <!-- Form view of PLan -->
    <record id="view_plan_form" model="ir.ui.view">
        <field name="name">view.standard.form</field>
        <field name="model">subscription.plan</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="%(action_user)d" type="action" string="Users"/>
                </header>
                <sheet>
                    <div name="button_box" class="oe_button_box">
                        <button class="oe_stat_button"
                                type="action"
                                name="%(action_plan_users)d"
                                context="{'default_plan_id':active_id,'search_default_plan_id':active_id}"
                                string="Users"
                                icon="fa-users"/>
                    </div>
                    <group>
                        <field name="name"/>
                        <field name="code"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Tree view of PLan -->
    <record id="view_plan_tree" model="ir.ui.view">
        <field name="name">view.plan.tree</field>
        <field name="model">subscription.plan</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name"/>
                <field name="code"/>
            </tree>
        </field>
    </record>

    <!-- Action of Plan -->
    <record id="action_plan" model="ir.actions.act_window">
        <field name="name">Plans</field>
        <field name="res_model">subscription.plan</field>
        <field name="view_mode">tree,form</field>
    </record>

    <!-- Menu of Plans -->
    <menuitem id="menu_plan" action="action_plan" parent="menu_configuration"/>

</odoo>