<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <record id="view_subscription_wizard_form" model="ir.ui.view">
        <field name="name">subscription.wizard.form</field>
        <field name="model">subscription.wizard</field>
        <field name="arch" type="xml">
            <form string="Subscription Wizard">
                <group>
                    <field name="user_id" invisible="1"/>
                    <field name="recurrence_id" widget="many2many_tags"/>
                    <field name="subscription_ids" widget="many2many_tags"/>
                </group>
                <footer>
                    <button name="action_open_subscriptions" string="Open" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_subscription_wizard" model="ir.actions.act_window">
        <field name="name">Subscription Wizard</field>
        <field name="res_model">subscription.wizard</field>
        <field name="view_mode">form</field>
        <field name="view_id" ref="view_subscription_wizard_form"/>
        <field name="target">new</field>
    </record>
</odoo>